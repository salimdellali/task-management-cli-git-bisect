# How to use `run-bisect.sh`
- The `run-bisect.sh` must me placed at the parent of the current working directory
- Then, to execute the script, you must run `../run-bisect.sh` from within the current working directory